>> theme integration
>> create default pages
    >> components
        >> header
        >> menu
        >> footer
    >> common pages
        >> index
        >> create
        >> edit
        >> show
        >> filter (list)
        >> pagination (list)
    >> lang
        >> en.json
        >> bn.json
    >> 
